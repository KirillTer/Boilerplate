import React from "react";

const AuthView = ({ auth }) => {
  return (
    <div className="container">
      {console.log("Female", auth)}
      <h1>Auth Page</h1>
    </div>
  );
};

export default AuthView;
