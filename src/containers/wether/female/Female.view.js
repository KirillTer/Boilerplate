import React from "react";

const FemaleView = ({ weather }) => {
  return (
    <div className="container">
      {console.log("Female", weather)}
      <h1>Female</h1>
    </div>
  );
};

export default FemaleView;
